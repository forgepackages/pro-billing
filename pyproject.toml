[tool.poetry]
name = "project"
version = "0.0.0"
description = ""
authors = []

[tool.poetry.dependencies]
python = "^3.9"

forge = "<1.0.0"

PyJWT = "^2.3.0"
cryptography = "^36.0.1"
forge-pro = {git = "ssh://git@github.com/django-forge/forge-pro.git", rev = "cc7e931"}


[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
